[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && cd ../ml && pip install -r requirements.txt && python train.py"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 60
